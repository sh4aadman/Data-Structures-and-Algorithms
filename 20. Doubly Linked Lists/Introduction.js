// Doubly Linked Lists - contains head, tail, length property.

// Consists of nodes - each node has a value, a pointer to next node or null and a pointer to previous node or null.

// Comparison with Arrays - do not have indeces.

// Connected via nodes with a next pointer and a previous pointer.

// Insertion and Deletion both at the begining and the end of the list are of constant time O(1).

// Doubly Linked Lists are an excellent choice for - browser history (back and forward), music playlists (skipping to the next/previous song), undo/redo in text editors.

// The idea of a list data structure that consists of nodes - Stacks and Queues.