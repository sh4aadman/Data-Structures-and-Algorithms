// Stack - A linear data structure following the Last-In-First-Out (LIFO) principle, where elements are added and removed only from the top.

// Stacks are an excellent choice for -
// 01. Function Call Management: JavaScript interpreter uses a call stack to manage function invocations, pushing a function onto the stack when it is called and popping it off when it returns.
// 02. Undo: Text editors use stacks to store the history of actions, allowing the most recent action to be reversed first.
// 03. Browser History: The "back" button in web browsers uses a stack to navigate the history of visited pages.
// 04. Expression Evaluation and Syntax Parsing: Compilers and interpreters use stacks to verify that parentheses and brackets are balanced in code or expressions.